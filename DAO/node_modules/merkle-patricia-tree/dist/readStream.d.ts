import { Trie as BaseTrie } from 'merkle-patricia-tree/dist/baseTrie';
declare const Readable: any;
export declare class TrieReadStream extends Readable {
    private trie;
    private _started;
    constructor(trie: BaseTrie);
    _read(): Promise<void>;
}
export {};
